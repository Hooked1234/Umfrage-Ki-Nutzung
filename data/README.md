# Umfragedaten

Lege hier die Umfragedaten ab:

- `umfrage.xlsx` – Excel-Export der Umfrage
- `umfrage.csv` – CSV-Export (alternativ)

> Die Datei wird von `pandas-mcp-server` eingelesen und vom Survey Evaluator ausgewertet.
